import React from 'react';

async function  Page({
                         params,
                     }: {
    params: { id: number };
}) {
    return (
        <div className="py-10 px-4">
            bought
        </div>);
}

export default Page;